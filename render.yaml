services:
  - type: web
    name: bone-fracture-webapp
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --workers=1 --threads=1 --timeout=120 app:app
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.13"